---
name: style-transfer
description: 根据 ArtShift 画廊编号或用户照片，生成风格迁移提示词和图片；未选编号时推荐临时原创方向。
version: 1.0.0
---

# ArtShift 风格迁移

工作区：`E:\projectHome\ArtShift`

先打开 `index.html`，或读取 `data/curated/prompts.json`。这里的编号对应已经整理好的公开风格库；抓取原始数据与抓取工具不属于本 Skill 的公开范围。

## 使用方式

- 用户提供编号：确认 `id` 存在，完整使用该条目的 `prompt`，仅补充用户上传照片和明确的主题要求。
- 用户未提供编号：不直接生成；推荐 2 个库内编号和 3 个临时原创方向 `a/b/c`，等待用户选择。
- 用户选择 `a/b/c`：该字母方案只在当前轮有效。

## 原创提示词规则

使用以下模板，且不要扩展其中的固定句：

`风格：【风格变量】。把原图艺术化成指定风格。视觉核心居中，和背景风格保持一致。四周{整数}%负空间（可选）。随机加一两条主题相关文案做成海报。（可选）`

风格变量轮流使用画风名称、艺术家名称、作品名称、材质/线条/颜色描述；避免重复。原创 `a/b/c` 只在当前轮有效，不写入公开编号库。参考图是主体与关键视觉线索来源，风格化结果必须是一张完整统一的画面。

## 交付

生成时明确以用户上传照片为主体参考；如保存成品，使用新文件名，不覆盖已有版本。交付时区分已生成、已检查和未验证结果。
